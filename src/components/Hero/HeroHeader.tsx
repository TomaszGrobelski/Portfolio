const HeroHeader = ({ className }: { className: string }) => {
  return (
    <h1 className={className}>
      <span>Tomasz</span>
      <span>Grobelski</span>
    </h1>
  );
};

export default HeroHeader;
